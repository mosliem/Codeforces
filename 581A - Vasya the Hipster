#include <bits/stdc++.h>
#include <algorithm>
using namespace std;

#define ll long long
int main()
{    
	int a, b;
	cin >> a >> b;
	if (a > b)
		swap(a, b);
	cout << a << " " << (b - a) / 2 << endl;
}
